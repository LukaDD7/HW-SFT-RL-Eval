# Manifests

`models.yaml` and `datasets.yaml` are placeholders until the Hugging Face
repositories are created.

Rules:

1. One canonical revision per model or asset.
2. Update the manifest before updating a runner default.
3. Never point to local paths in a manifest.
4. Keep raw data and weights out of Git.
